# write a script in this file that finds all numbers between 1 and 1000 that when reversed, and added to itself, create a numeric palindrome.
require './lib/number'

numbers = Number.new
 numbers.find_palindrome_values
 p numbers.find_palindromes
